#!/bin/bash
# Generated by Chef for <%= @fqdn %>
# Local modifications will be overwritten.
RUBY_DIR="/usr/local/ruby/<%= @ruby_dir %>"
if ! echo ${PATH} | /bin/grep -q $RUBY_DIR/bin ; then
   PATH=$RUBY_DIR/bin:${PATH}
fi
